<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <% const name = 'Xavier' %>
    <p><%= name %></p>
    <p>Hi, trying out node js and home page</p>

    <p>ALL BLOGS</p>
    <% if(blogs.length>0){ %> <% blogs.forEach(blog=>{%>
    <h3><%= blog.title %></h3>
    <h2><%= blog.snippet %></h2>
    <h2><%= blog.body %></h2>
    <% }) %> <% } else { %>
    <p>There are no blogs available</p>
    <% } %>
  </body>
</html>
